---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: svc-identity
spec:
  replicas: {{ .Values.resources.replicas }}
  selector:
    matchLabels:
      type: infra
      app: svc-identity
  template:
    metadata:
      labels:
        type: infra
        app: svc-identity
    spec:
      containers:
        - name: svc-identity
          image: "{{.Values.image.repository}}:{{.Values.image.tag}}"